/**
 * It defines compare operators.
*/
export declare enum CompareOperator {
    /**
     * must be less than
     */
    LessThan = 0,
    /**
     * cannot be more than
     */
    LessThanEqual = 1,
    /**
     *  must be the same as
     */
    Equal = 2,
    /**
     * must be different from
     */
    NotEqual = 3,
    /**
     * cannot be less than
     */
    GreaterThanEqual = 4,
    /**
     * must be more than
     */
    GreaterThan = 5,
}
